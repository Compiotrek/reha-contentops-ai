# Copy this file to .env for local LLM runs.
# Never commit .env.
OPENAI_API_KEY=
# Optional. If omitted, the app falls back to gpt-5.4-nano.
OPENAI_MODEL=gpt-5.4-nano
# Optional. If omitted, embedding matching falls back to text-embedding-3-small.
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
